@model IEnumerable<WEB_LBA.Models.M_Ciclo>

@{
    ViewBag.Title = "Ciclos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section styles {
    <link href="~/Content/Ciclos.css" rel="stylesheet" />
}

<div class="contenedor-lista">
    <div class="encabezado">
        <h2>Lista de Ciclos</h2>
        @Html.ActionLink("➕ Crear Ciclo", "AgregarCiclos", null, new { @class = "btn btn-success btn-crear" })
    </div>

    <!-- Tabla de Ciclos -->
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>ID Ciclo</th>
                <th>Nombre</th>
                <th>Descripción</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.id_ciclo</td>
                    <td>@item.nombre_ciclo</td>
                    <td>@item.descripcion</td>
                    <td>
                        <div class="action-buttons">
                            @Html.ActionLink("✏️ Modificar", "ModificaCiclos", new { id = item.id_ciclo }, new { @class = "btn btn-warning btn-sm" })
                            @Html.ActionLink("🗑️ Eliminar", "EliminaCiclos", new { id = item.id_ciclo }, new { @class = "btn btn-danger btn-sm" })
                            @Html.ActionLink("📘 Ver Secciones", "VerSeccionesPorCiclo", "Seccion", new { id = item.id_ciclo }, new { @class = "btn btn-info btn-sm" })
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- Botones de acción -->
    <div class="botones-acciones">
        <button type="submit" class="btn btn-primary">Agregar</button>
        @Html.ActionLink("Volver a Panel Administrativo", "PanelAdministrativo", "Home", null, new { @class = "btn btn-secondary" })
    </div>
</div>
